#%%
import pandas as pd
import seaborn as sns
# %%
df = pd.read_csv("data.csv")
df.head()
# %%
def rename(x :str):
    if x.startswith("_"):
        return x[1:]
    return x
    
df = df.rename(columns=rename)
df.head()
# %%
sns.pairplot(df, 
             y_vars=["best_bound","linear_nonzeros","miprelgap","n_iterations","n_nodes_processed","ncolumns","constraints"],
             x_vars=["time"],
             kind="reg")
